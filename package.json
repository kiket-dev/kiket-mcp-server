{
  "name": "@kiket/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Kiket MCP tools over the authorized platform API model",
  "type": "module",
  "bin": {
    "kiket-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "lint": "biome check --diagnostic-level=error src/ tests/",
    "test": "vitest run tests/tools.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kiket/api-client": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/node": "^24.12.4",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
